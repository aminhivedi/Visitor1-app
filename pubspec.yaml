name: my_sales_app
description: A complete Flutter app for sales visitors.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  geolocator: ^10.1.0
  Maps_flutter: ^2.5.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  flutter_secure_storage: ^9.0.0
  intl: ^0.18.1
  permission_handler: ^11.0.1
  flutter_localizations:
    sdk: flutter
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
